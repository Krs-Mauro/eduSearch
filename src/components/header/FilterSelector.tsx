const FilterSelector = () => {
  return (
    <div className="flex justify-between items-center font-medium">
      <p>Filter By:</p>
      {/* map over degree types */}
    </div>
  )
}

export default FilterSelector
